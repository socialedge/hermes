import angular from 'angular';
import PublicationsPageComponent from './publications-page.component';

export default angular.module('PublicationsPage', []).component(PublicationsPageComponent.name, new PublicationsPageComponent);
