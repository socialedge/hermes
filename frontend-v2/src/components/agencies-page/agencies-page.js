import angular from 'angular';
import AgenciesPageComponent from './agencies-page.component';

export default angular.module('AgenciesPage', []).component(AgenciesPageComponent.name, new AgenciesPageComponent);
